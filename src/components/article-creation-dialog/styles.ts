import { styled } from '../../../stitches.config'

const Footer = styled('footer', {
  display: 'flex',
  gap: '0.75rem',
  alignItems: 'center',
  justifyContent: 'flex-end',
  marginTop: '0.75rem',
})

export { Footer }
